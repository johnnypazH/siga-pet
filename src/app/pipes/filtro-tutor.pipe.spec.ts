import { FiltroTutorPipe } from './filtro-tutor.pipe';

describe('FiltroTutorPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTutorPipe();
    expect(pipe).toBeTruthy();
  });
});